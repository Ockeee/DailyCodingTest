class Solution {
    public int solution(String dartResult) {
        int answer = 0;
        return answer;
    }
}

// dartResult 3분할. arraylist 에 넣어서 인덱스 부여. 
// 정규표현식 영어나 #, * 있으면 자르기. (\\d+)([SDT])([#*]?)

// 점수 부여해줄 arraylist 선언.
// 1,2,3과 D,S,T 를 보고 점수 부여. power 함수가 있나 
// 만약 *가 있으면 현재 인덱스와 현재 인덱스 -1 까지 *2 해주기.
// # 이 있으면 현재 인덱스 * -1 하기. 
